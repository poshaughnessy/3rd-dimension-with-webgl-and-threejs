/* This is the first TRex script that must be called, in order to create the namespace */
/* In a production app with a build process, this could just go at the top of the built JS file, as with Three.js */

var TREX = TREX || {REVISION: '1.0'};
